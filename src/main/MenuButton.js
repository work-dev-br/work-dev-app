import React from 'react';
import './MenuButton.css';

function MenuButton() {
    return (
        <div className="MenuButton">
            <p>--</p>
        </div>
    );
}

export default MenuButton;
